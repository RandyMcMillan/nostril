name: macos-pre-release

# Controls when the action will run.
on:
  push:
    branches:
      - 'master'
      - '**pre-release**'

  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  pre-release:
    strategy:
      matrix:
        os: [macos-11, macos-12, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: styfle/cancel-workflow-action@0.9.1
        with:
          access_token: ${{ github.token }}
      - uses: actions/checkout@v2
      - name: "printenv"
        run: |
          brew install libtool autoconf automake
          printenv
          UNAME_M=$(echo uname -m)
          export UNAME_M
          echo $UNAME_M
      - name: "build pre-release $(echo $UNAME_M)"
        run: |
          make
          cp nostril nostril-$UNAME_M
          shasum -a 256 nostril-$UNAME_M > nostril-$UNAME_M.sha256.txt
      - name: Save state
        run: echo "{name}={value}" >> $GITHUB_STATE
      - name: Set output
        run: echo "{name}={value}" >> $GITHUB_OUTPUT
      - uses: "marvinpinto/action-automatic-releases@latest"
        with:
          repo_token: "${{ secrets.GITHUB_TOKEN }}"
          automatic_release_tag: ${{ matrix.os }}
          prerelease: true
          title: "Development Build $(echo uname -m)"
          files: |
            nostril-$UNAME_M
            nostril-$UNAME_M.sha256.txt
